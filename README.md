# Bombardinha

Tutorial:

```bash
# Install Poetry.
pip install poetry

# Initialize the project.
poetry install

# Run the project.
poetry run python -m bombardinha.main

# Add dependencies.
poetry add <dependency_name>
```
